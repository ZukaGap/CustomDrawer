# In the repo root folder.

# Install dependencies.

yarn

# Move to ios folder.

cd ios

# Install pod dependence.

pod install && cd ..

# Start the iOS app.

yarn ios

# Start the android app.

yarn android
